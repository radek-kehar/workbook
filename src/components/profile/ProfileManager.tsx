import {ValidationProvider} from "../form/validation/ValidationProvider";
import React from "react";

const ProfileManager = () => {
    // todo: vytvorit profil, zmenit profil, smazat profil, vybrat profil
    return (
        <div>

        </div>
    )
}

export default ProfileManager;
